# SonarCloud configuration for register-early-career-teachers-public

# Exclude test files from duplication detection
sonar.cpd.exclusions=spec/**/*

# Source encoding
sonar.sourceEncoding=UTF-8

# Source directories
sonar.sources=app,lib,config

# Test directories
sonar.tests=spec

# Exclude patterns
sonar.exclusions=node_modules/**/*,vendor/**/*,tmp/**/*,log/**/*,public/**/*,storage/**/*

# Allow empty controller actions (e.g. those that only render a view)
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=ruby:S1186
sonar.issue.ignore.multicriteria.e1.resourceKey=app/controllers/schools/reminder_email_opt_outs_controller.rb
