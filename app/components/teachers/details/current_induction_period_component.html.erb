<h2 class="govuk-heading-m">Current induction period</h2>

<%=
  govuk_summary_list(card: { title: current_period.appropriate_body.name, actions: }) do |sl|
    sl.with_row do |row|
      row.with_key(text: "Induction programme")
      row.with_value(text: helpers.induction_programme_choice_name(current_period.induction_programme))
    end

    sl.with_row do |row|
      row.with_key(text: "Start date")
      row.with_value(text: started_on)
    end
  end
%>

TODO: custom CSS with negative margin spacing
<p class='govuk-!-text-align-right govuk-!-margin-bottom-0'>
  <%= govuk_link_to('Add an induction period', new_admin_teacher_induction_period_path(teacher_id: teacher.id)) %>
</p>
