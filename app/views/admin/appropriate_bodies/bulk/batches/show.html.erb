<%
  page_data(
    title: "CSV upload by #{@appropriate_body.name}",
    backlink_href: admin_appropriate_body_bulk_batches_path(@appropriate_body)
  )
%>

<%=
  render partial: @pending_induction_submission_batch.batch_type,
  locals: { batch: @pending_induction_submission_batch }
%>