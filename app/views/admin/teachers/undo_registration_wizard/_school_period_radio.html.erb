<%= form.govuk_radio_button(
  :at_school_period_gid,
  at_school_period.to_global_id.to_s,
  label: { text: at_school_period.school.name },
  hint: -> {
    safe_join(
      [
        appropriate_body_name.presence,
        "#{at_school_period.started_on.to_fs(:govuk)} to #{at_school_period.finished_on&.to_fs(:govuk) || "present"}"
      ].compact,
      tag.br
    )
  },
  link_errors:
) %>
