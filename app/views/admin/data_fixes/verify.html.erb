<% page_data(
    title: "Confirm the proposed changes",
    error: @wizard.current_step.errors.any?,
    backlink_href: @wizard.previous_step_path
) %>

<%= form_with(
      model: @wizard.current_step,
      url: @wizard.current_step_path,
      scope: @wizard.current_step_name
    ) do |form| %>
  <%= content_for(:error_summary) do %>
    <%= form.govuk_error_summary(presenter: @wizard.error_presenter) %>
  <% end %>

  <%= render Admin::DataFixes::ProcessedChangesComponent.new(@wizard.current_step.processed_changes) %>

  <%= render Admin::ContextFormFieldsComponent.new(form:) %>

  <%= form.govuk_submit "Confirm changes" %>
<% end %>
