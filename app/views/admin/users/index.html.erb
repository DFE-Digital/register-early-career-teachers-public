<%
  page_data(
    title: 'DfE staff',
    error: false
  )
%>

<p class="govuk-body">
  <%= govuk_button_link_to 'Add new user', new_admin_user_path, secondary: true %>
<p>

<%= govuk_list(@users.map { |u| govuk_link_to(u.name, admin_user_path(u), no_visited_state: true) }) %>
