<% page_data(
  title: "Teacher does not have QTS",
  error: true,
  backlink_href: new_admin_register_ect_path)
%>

<h1 class="govuk-heading-l">Teacher does not have QTS</h1>

<p class="govuk-body">
  <%= @pending_induction_submission.trs_first_name %> <%= @pending_induction_submission.trs_last_name %> (TRN: <%= @pending_induction_submission.trn %>) does not have qualified teacher status (QTS) awarded.
</p>

<p class="govuk-body">
  You cannot import teacher records for teachers who have not yet been awarded QTS.
</p>

<h2 class="govuk-heading-m">What you can do</h2>

<ul class="govuk-list govuk-list--bullet">
  <li><%= govuk_link_to("Import a different teacher", new_admin_register_ect_path) %></li>
  <li><%= govuk_link_to("Return to teacher list", admin_teachers_path) %></li>
</ul>
