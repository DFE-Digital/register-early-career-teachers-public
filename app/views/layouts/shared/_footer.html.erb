<%=
  govuk_footer(
    meta_items: {
      'Accessibility' => '/accessibility',
      'Cookies' => '/cookies',
      'Privacy' => '/privacy',
      'Contact support' => '/support'
    }
  )
%>

<%=
  render DebugSessionComponent.new(
    current_session: controller.session['user_session'],
    current_user:
  )
%>
