<h1 class='govuk-heading-l'>CSV file summary</h1>

<% if batch.no_valid_data? %>
  <p class='govuk-body'>
    Your CSV named '<%= batch.filename %>' has <%= pluralize(batch.pending_induction_submissions.with_errors.count, 'ECT') %> with errors.
  </p>

  <p class='govuk-body'>
    <%= govuk_link_to 'Download CSV with error messages included', ab_batch_claim_path(batch, format: :csv) %>.
    You'll need to fix these errors before you try again.
  </p>

  <p class='govuk-body'>
    <%= govuk_link_to 'Go back to your overview', ab_teachers_path %>
  </p>

<% else %>
  <p class='govuk-body'>
    Your CSV named '<%= batch.filename %>' had a total <%= pluralize(batch.data.count, 'ECT record') %> in one year claims.
  </p>

  <p class='govuk-body'>
    You have <%= pluralize(batch.pending_induction_submissions.without_errors.count, 'ECT') %> without errors. You can download a link to process these below or fix any messages indicated and try again.
  </p>

  <% if batch.pending_induction_submissions.with_errors.any? %>
    <p class='govuk-body'>
      You had <%= pluralize(batch.pending_induction_submissions.with_errors.count, 'ECT') %> with errors which were not processed.
    </p>

    <p class='govuk-body'>
      <%= govuk_link_to 'Download CSV with error messages included', ab_batch_claim_path(batch, format: :csv) %>.
      You'll need to fix these errors before you try again.
    </p>
  <% end %>

  <p class='govuk-body'>
    <%= govuk_link_to 'Go back to your overview', ab_teachers_path %>
  </p>
<% end %>
