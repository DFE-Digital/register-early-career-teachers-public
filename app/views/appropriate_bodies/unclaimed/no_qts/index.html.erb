<% page_data(
  title: "ECTs without qualified teacher status (QTS)",
  backlink_href: ab_unclaimed_path
) %>

<p class="govuk-body">
  You cannot claim these ECTs until their QTS award has been verified by the school.
</p>
<%= govuk_section_break(visible: false, size: "l") %>

<%= render UnclaimedIndex::SearchSectionComponent.new(
  query: @query,
  form_url: ab_unclaimed_no_qts_path
) %>

<%= render UnclaimedIndex::TableSectionComponent.new(
  ect_at_school_periods: @ect_at_school_periods,
  pagy: @pagy,
  row_component: UnclaimedIndex::NoQts::TableRowComponent
) %>
